package main;

public class OrderExecutor {
    public OrderExecutor(Order order, Account account) {
    }

    public Order processTrade(Order order) {
        // 1. Get Current Price
        // 2. Get current time
        // 3. Ping Transaction manager
        // Idk if this needs to return an Order object
        return order;
    }
}
